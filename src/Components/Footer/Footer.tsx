import React from "react";
import { Box, Container, Typography } from "@mui/material";

function Footer() {
  return (
    <Container
      sx={{
        width: "100%",
        margin: "auto",
        textAlign: "center",
        alignItems: "center",
        backgroundColor: "#D991CF",
      }}
    >
      <Typography>Medium: @sevdasevinu</Typography>
    </Container>
  );
}

export default Footer;
